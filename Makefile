# Makefile IRL Quest

Makefile содержит команды для сборки, запуска и тестирования проекта:

- `make dev-up` — запуск всех сервисов в режиме разработки
- `make dev-down` — остановка всех сервисов
- `make migrate` — применение миграций базы данных
- `make lint` — запуск линтеров
- `make test` — запуск тестов
- `make build` — сборка Docker-образов
- `make seed` — заполнение тестовыми данными

_Подробнее смотрите сам файл Makefile._