apiVersion: v1
kind: Service
metadata:
  name: {{ include "irl-quest.fullname" . }}
  labels:
    {{- include "irl-quest.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    {{- include "irl-quest.selectorLabels" . | nindent 4 }}

